Al realizar una petición POST a la ruta '/registro' enviando un JSON con los campos 'nombre' y 'mensaje', el servidor procesa esta información utilizando 'req.body' y responde con un objeto JSON que confirma el estado 'Datos recibidos', devolviendo los mismos valores enviados. Esto comprueba que la API recibe, interpreta y procesa correctamente los datos de entrada.
